# デプロイ・テスト方針 — Auto-Reward-Service

## 方針決定

Unit ごとのデプロイ・試験は以下の **ハイブリッド方式** で実施する。
基盤系（U0〜U2）は段階的に毎回デプロイ＋疎通確認し、ビジネスロジック系（U3〜U7）はまとめてデプロイする。

---

## デプロイ・テストタイミング

| デプロイ回 | 対象 Unit | 確認内容 | 確認方法 |
|-----------|----------|---------|---------|
| **1回目** | U0 | SAM デプロイ成功 / DynamoDB テーブル作成 / Layer デプロイ | `sam deploy` → AWS コンソールで ArsTable 確認 |
| **2回目** | U0 + U1 | LINE Webhook 疎通 / 署名検証 / エコー応答 | LINE アプリから「こんにちは」送信 → 応答確認 |
| **3回目** | U0 + U1 + U2 | リワードちゃん口調の応答 / Bedrock 実呼び出し / Intent 分類 | LINE で「疲れた」→ キャラ口調で返事を確認 |
| **4回目** | U0〜U5 | 支出記録 → 候補プール → ご褒美提案の E2E チェーン | LINE で「プリン買った 320円」→ 支出記録 → 「疲れた」→ ご褒美提案 |
| **5回目** | U0〜U7 | Push 通知 / LIFF ダッシュボード | EventBridge 手動実行 → Push 確認 / LIFF URL アクセス |

---

## 各 Unit の Code Generation 時の指示

各 Unit のコード生成が完了した際、以下を含めること：

1. **ユニットテスト**: そのスライスのテストコードを含める（従来通り）
2. **デプロイ確認手順**: 上表の該当デプロイ回に対応する確認手順をドキュメントとして出力する
3. **sam deploy の差分**: template.yaml への追加リソース（Lambda 関数・イベントソース等）を明確にする

基盤系（U0〜U2）は各 Unit 完了ごとにデプロイ確認を実施する。
ビジネスロジック系（U3〜U5、U6、U7）はまとめてデプロイし、E2E シナリオで確認する。

---

## 理由

- **U0〜U2 は毎回デプロイ必須**: インフラ基盤 → 外部サービス接続 → デモの核心（リワードちゃん応答）と段階的に積み上がる構造。土台が壊れていると全部動かない
- **U3〜U5 はまとめてOK**: 支出記録 → 候補プール → ご褒美提案は一連のチェーン。3つ揃ってはじめて E2E で検証できる
- **sam deploy は差分デプロイ**: 毎回全ビルドではないのでオーバーヘッドは小さい
- **合計5回のデプロイで全機能確認可能**: ハッカソンのスピード感を維持しつつ品質を担保

## その他
明確な用件な場合は承認をスキップして次の作業に進むことも許可する
ただし、AI-DLC成果物はすべて確実に生成すること