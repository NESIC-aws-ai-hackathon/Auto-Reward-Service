# Nova Act SDK (Python 3.10+ 必須)
nova-act>=3.0,<4.0

# Pydantic は act_get の schema 構築で必須
pydantic>=2.0,<3.0

# AWS 連携 (ECS Worker モード)
boto3>=1.34

# Login Server (Flask)
flask>=3.0,<4.0
gunicorn>=21.0
